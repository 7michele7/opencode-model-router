{
  "name": "opencode-model-router",
  "version": "1.0.0",
  "private": true,
  "description": "Automatic model selection for OpenCode based on task complexity",
  "type": "module",
  "scripts": {
    "test": "node --experimental-strip-types --no-warnings test/core.test.ts",
    "install-plugin": "./install.sh",
    "test:e2e": "node --experimental-strip-types --no-warnings test/failure.e2e.ts"
  }
}
